#include "main.h"
/**
*_abs- print_ - Prints the alphabet in lowercase 10 times.
*@i: the number input in the function
* Return: Always 0 (Success)
*/
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
